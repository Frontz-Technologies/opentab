node_modules/
.next/
dist/
.turbo/
.env
.env.local
.research/
.env.dev
*.log
.DS_Store
coverage/
.vercel/
docs/superpowers/
test-results/
playwright-report/
.test-user.json
*.tsbuildinfo

# Process core dumps from segfaulted test runners / dev servers.
# Matches the legacy `core` filename plus Linux's `core.<pid>` pattern.
core
core.*
# The CSV import engine (#215) lives at apps/web/lib/import/core/ —
# keep tracked. Negation must come AFTER the broad rules above.
!apps/web/lib/import/core/
!apps/web/lib/import/core/**
