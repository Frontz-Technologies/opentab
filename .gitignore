node_modules/
.next/
dist/
.turbo/
.env
.env.local
.research/
.env.dev
*.log
.DS_Store
coverage/
.vercel/
docs/superpowers/
test-results/
playwright-report/
.test-user.json
*.tsbuildinfo

# Process core dumps from segfaulted test runners / dev servers.
# Matches the legacy `core` filename plus Linux's `core.<pid>` pattern.
# If a directory called `core/` is ever needed, add `!core/` to override.
core
core.*
